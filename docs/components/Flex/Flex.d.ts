import { FlexProps } from './Flex.props';

declare const Flex: ({ className, children, testId, m, mx, my, mt, mr, mb, ml, p, px, py, pt, pr, pb, pl, h, hMin, hMax, w, wMin, wMax, bgColor, border, borderRadius, borderTop, borderBottom, direction, wrap, grow, shrink, basis, flex, gap, gapX, gapY, justify, align, alignContent, alignSelf, order, ...htmlProps }: FlexProps) => import("react/jsx-runtime").JSX.Element;
export { Flex };
