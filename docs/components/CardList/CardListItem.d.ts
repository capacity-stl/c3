import { CardListItemProps } from './CardList.props';

export declare const CardListItem: <T extends Record<string, any>>({ item, titleKey, descriptionKey, datetimeKey, referenceKey, isSelected, bottomLeftContent, bottomRightContent, firstOptionAsButton, textSelectable, }: CardListItemProps<T>) => import("react/jsx-runtime").JSX.Element;
