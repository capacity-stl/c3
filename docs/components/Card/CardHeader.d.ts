import { CardHeaderProps } from './Card.props';

declare const CardHeader: ({ className, children, title, description, ...cardProps }: CardHeaderProps) => import("react/jsx-runtime").JSX.Element;
export { CardHeader };
