import { PaginationProps } from './Pagination.props';

export declare const usePaginationColors: (props: PaginationProps) => {
    itemColors: {
        bgColor: string;
        textColor: string;
        borderColor: string;
    };
    activeColors: {
        bgColor: string;
        textColor: string;
        borderColor: string;
    };
    hoverColors: {
        bgColor: string;
        textColor: string;
        borderColor: string;
    };
    nextPreviousColors: {
        bgColor: string;
        borderColor: string;
        textColor: string;
        hoverBgColor: string;
        hoverBorderColor: string;
        hoverTextColor: string;
    };
};
