import { CollapsibleSheetProps } from './CollapsibleSheet.props'

const CollapsibleSheet = (props: CollapsibleSheetProps) => {
  const { children } = props

  return <>{children}</>
}

export { CollapsibleSheet }
